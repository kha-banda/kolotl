version: '3.8'

services:
  app:
    build: .
    container_name: flask_app
    ports:
<<<<<<< HEAD
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py
    command: flask run --host=0.0.0.0 --port=5000"
=======
      - "5000:80"
    volumes:
      - /app
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.app.rule=Host(`myapp.traefik.me`)"  # Aquí el dominio subido de Traefik
      - "traefik.http.services.app.loadbalancer.server.port=80"  # Asegúrate de exponer el puerto correcto
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py
    command: sh -c "ls -R /app && flask run --host=0.0.0.0 --port=80"
>>>>>>> 750333618eb323050f6e810a0739d5d58afea2ac

